#include <stdio.h>   // Biblioteca padrão para funções de entrada e saída (printf, scanf)

int main(void)        // Função principal; onde o programa começa
{
    // Imprime "hello, world" na tela e pula para a linha seguinte
    printf("hello, world\n");

    // Retorna 0 indicando que o programa terminou sem erros
    return 0;
}
